import { ReactElement } from "react";

export interface newServiceProps {
  Image: ReactElement;
  Title: string;
  Description: string;
  Button: ReactElement;
  BackGroundFrom: string;
  BackGroundTo: string;
  ColorText: string;
}
